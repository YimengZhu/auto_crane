controller_list:
  - name: fake_gripper_group_controller
    type: $(arg fake_execution_type)
    joints:
      - base_arm
      - arm_slider
      - slider_gripper
initial:  # Define initial robot poses.
  - group: gripper_group
    pose: home